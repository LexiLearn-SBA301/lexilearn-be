spring:
  mail:
    username: ${MAIL_USERNAME:test@example.com}
    password: ${MAIL_PASSWORD:test-mail-password}

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:test-cloud}
  api-key: ${CLOUDINARY_API_KEY:test-key}
  api-secret: ${CLOUDINARY_API_SECRET:test-secret}

app:
  jwt:
    secret: ${JWT_SECRET:test-jwt-secret-minimum-32-bytes-long}
